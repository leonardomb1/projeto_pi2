export default class returnClass {
    constructor(message, status, success, error, data) {
        this.message = message,
        this.status = status,
        this.success = success,
        this.error = error,
        this.data = data
    }
}